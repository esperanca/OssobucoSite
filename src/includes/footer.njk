<footer class="footer">
    <div>
    {% if meta.email %}
        <p><a href="mailto:{{ meta.email }}">{{ meta.email }}</a></p>
    {% endif %}
    {% if meta.telephone %}
        <p><a href="tel:{{ meta.telephone }}">{{ meta.telephone }}</a></p>
    {% endif %}
    </div>
    <div>
        <p>Atualizado em: {{ build.timestamp | dateToFormat("dd mm yy, t") }}</p>
    </div>
</footer>